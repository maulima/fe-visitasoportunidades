import { StyleSheet } from 'react-native'

import { BACKGROUND_COLOR, GREEN, OCEAN_BLUE, WHITE } from '../Colors'

const styles: any = StyleSheet.create({
    container: {
        width: 150,
        height: 150,
        backgroundColor: WHITE
    }
})